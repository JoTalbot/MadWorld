diff --git a/.github/workflows/release-gate.yml b/.github/workflows/release-gate.yml
--- a/.github/workflows/release-gate.yml
+++ b/.github/workflows/release-gate.yml
@@ -10,6 +10,14 @@
       - 'ops/**'
       - 'docs/**'
       - '.github/workflows/**'
+
+# Only the newest candidate for a ref may occupy release-gate runners.
+concurrency:
+  group: ${{ github.workflow }}-${{ github.ref }}
+  cancel-in-progress: true
+
+permissions:
+  contents: read

 jobs:
   backend:
@@ -89,8 +97,10 @@
   android-matrix:
     name: Android API ${{ matrix.api }} smoke
     runs-on: ubuntu-latest
+    timeout-minutes: 45
     strategy:
       fail-fast: false
+      max-parallel: 2
       matrix:
         api: [26, 29, 32, 35]
     defaults:
@@ -110,39 +120,36 @@
       - name: Set up Android SDK
         uses: android-actions/setup-android@v3
       - name: Install Android SDK emulator
+        timeout-minutes: 10
         working-directory: .
         run: |
           set -euo pipefail
           yes | sdkmanager --licenses >/dev/null || true
           sdkmanager "platform-tools" "emulator" "system-images;android-${{ matrix.api }};google_apis;x86_64"
           echo "no" | avdmanager create avd -n "madworld-api-${{ matrix.api }}" -k "system-images;android-${{ matrix.api }};google_apis;x86_64" --force
-      - name: Boot emulator
+      - name: Enable KVM access on the GitHub-hosted runner
+        timeout-minutes: 1
         working-directory: .
         run: |
           set -euo pipefail
-          emulator -avd "madworld-api-${{ matrix.api }}" -no-window -no-audio -no-boot-anim -gpu swiftshader_indirect >"$RUNNER_TEMP/emulator-${{ matrix.api }}.log" 2>&1 &
-          adb wait-for-device
-          adb shell settings put global window_animation_scale 0
-          adb shell settings put global transition_animation_scale 0
-          adb shell settings put global animator_duration_scale 0
-          timeout 180 bash -c 'until adb shell getprop sys.boot_completed | grep -m1 "1"; do sleep 2; done'
-          adb shell input keyevent 82
-      - name: Install and launch APK
+          if [[ -c /dev/kvm && ( ! -r /dev/kvm || ! -w /dev/kvm ) ]]; then
+            timeout --kill-after=2 10 sudo -n chmod a+rw /dev/kvm
+          fi
+      - name: Run bounded Android emulator smoke
+        timeout-minutes: 15
         working-directory: .
         run: |
-          set -euo pipefail
-          adb install -r android/app/build/outputs/apk/debug/app-debug.apk
-          adb shell am force-stop com.jotalbot.madworld
-          adb shell monkey -p com.jotalbot.madworld 1
-          sleep 5
-          adb shell pidof com.jotalbot.madworld
-          adb shell dumpsys package com.jotalbot.madworld | grep -E 'versionName|versionCode'
-      - name: Upload emulator log on failure
-        if: failure()
+          bash ops/android_emulator_smoke.sh "${{ matrix.api }}" \
+            android/app/build/outputs/apk/debug/app-debug.apk \
+            "$RUNNER_TEMP/android-smoke-${{ matrix.api }}"
+      - name: Upload Android smoke evidence
+        if: always()
+        timeout-minutes: 5
         uses: actions/upload-artifact@v4
         with:
-          name: android-emulator-log-${{ matrix.api }}
-          path: ${{ runner.temp }}/emulator-${{ matrix.api }}.log
+          name: madworld-android-smoke-${{ matrix.api }}
+          path: ${{ runner.temp }}/android-smoke-${{ matrix.api }}/
+          if-no-files-found: error

   gate:
     runs-on: ubuntu-latest
